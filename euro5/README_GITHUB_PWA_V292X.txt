Easy Gym PWA v292X — GitHub Pages package

Built directly from PWA v291X.

Changes in v292X:
- MGMT, SUPPORT and INFO typography is restored exactly to the v290X presentation.
- Every visible weight unit is uppercase KG, including LOG, INFO and Word/PDF exports.
- GO no-data state collapses the hidden toolbar and Stop/Save row so the empty message aligns directly below the calendar.
- Exercise-picker uppercase names and all other approved v291X changes remain intact.
- PLAN, GO workout logic, LOG data, storage, sharing, languages, icons and click handlers remain unchanged.

Upload the contents of this ZIP directly to the GitHub Pages publishing root.
